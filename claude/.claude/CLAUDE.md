# Git — hard rules, every session

Never add attribution or tooling signatures to commits, PR descriptions,
or code comments.

Write commits like a developer:
- Subject: short, imperative, says what changed —
  "Remove empty file", "Fix csv logo upload", "Add retry on 502".
- Body (only when needed): prose explaining *why*, not bullets of *what*.
